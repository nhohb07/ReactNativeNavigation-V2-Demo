import { StyleSheet, Platform } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1
  },
  listContainer: {
    padding: 10
  },
  listLoading: {
    flex: 1,
    paddingTop: 20,
    paddingBottom: 30
  },
  emptyText: {
    textAlign: 'center'
  }
});